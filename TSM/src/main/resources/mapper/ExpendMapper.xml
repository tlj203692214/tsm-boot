<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tsm.mapper.ExpendMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tsm.entity.Expend">
        <id column="EXPEND_ID" property="expendId" />
        <result column="EXPEND_MONEY" property="expendMoney" />
        <result column="EXPEND_DATE" property="expendDate" />
        <result column="PAYAPPROVAL_ID" property="payapprovalId" />
        <result column="DELETED" property="deleted" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        EXPEND_ID, EXPEND_MONEY, EXPEND_DATE, PAYAPPROVAL_ID, DELETED
    </sql>

</mapper>
